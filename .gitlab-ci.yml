master:
    script:
        - mvn clean source:jar deploy -B -U -DforkCount=0
    tags:
        - mvn
    only:
        - master

branches:
    script:
        - mvn test -B
    tags:
        - mvn
    only:
        - branches
    except:
        - master
